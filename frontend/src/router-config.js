// React Router future flags configuration
import { UNSAFE_UNSAFE_enhanceManualRouteObjects } from 'react-router-dom';

// Opt into future flags to remove warnings
export const routerFutureConfig = {
  v7_startTransition: true,
  v7_relativeSplatPath: true
};

export default routerFutureConfig;
